# 购物网站实验报告

**学号：** [请填写学号]  
**姓名：** [请填写姓名]  
**项目名称：** 在线购物网站  
**实验日期：** [请填写日期]

---

## 1. 系统设计

### 1.1 系统整体架构

本购物网站采用经典的MVC（Model-View-Controller）架构模式，基于前后端分离的技术架构：

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   前端界面层     │    │   后端服务层     │    │   数据存储层     │
│   (HTML/CSS/JS) │◄──►│ (Node.js/Express)│◄──►│   (MySQL)       │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

### 1.2 技术栈

- **前端技术：** HTML5, CSS3, 原生JavaScript, 响应式设计
- **后端技术：** Node.js, Express.js框架
- **数据库：** MySQL数据库 + Sequelize ORM
- **认证方式：** JWT Token认证
- **开发工具：** VS Code, Git版本控制

### 1.3 系统模块介绍

#### 1.3.1 用户认证模块
- **功能：** 用户注册、登录、密码管理、用户信息维护
- **特点：** 使用bcrypt加密存储密码，JWT token实现无状态认证
- **路由：** `/api/auth/register`, `/api/auth/login`, `/api/auth/me`, `/api/auth/change-password`

#### 1.3.2 产品管理模块
- **功能：** 产品展示、产品搜索、库存管理
- **特点：** RESTful API设计，支持产品搜索和分页
- **路由：** `/api/products`, `/api/products/search/:keyword`

#### 1.3.3 购物车模块
- **功能：** 添加商品到购物车、购物车管理、数量调整
- **特点：** 基于用户会话的购物车，与用户账户绑定
- **路由：** `/api/cart/add`, `/api/cart`, `/api/cart/update`, `/api/cart/remove`

#### 1.3.4 订单管理模块
- **功能：** 订单创建、订单查询、订单状态管理
- **特点：** 完整的订单流程，支持订单历史查询
- **路由：** `/api/orders`, `/api/orders/create`, `/api/orders/:id`

#### 1.3.5 管理员模块
- **功能：** 产品管理、订单管理、销售报表、用户管理
- **特点：** 基于角色权限控制，提供完整的管理功能
- **路由：** `/api/admin/products`, `/api/admin/orders`, `/api/admin/reports`

### 1.4 数据库设计

#### 主要数据表：
- **users** - 用户信息表（用户ID、姓名、邮箱、密码、角色等）
- **products** - 产品信息表（产品ID、名称、价格、描述、库存、图片等）
- **cart_items** - 购物车表（购物车ID、用户ID、产品ID、数量等）
- **orders** - 订单表（订单ID、用户ID、订单状态、总金额等）
- **order_items** - 订单详情表（订单详情ID、订单ID、产品ID、数量、单价等）

---

## 2. 代码实现

### 2.1 项目结构

```
shopping-website/
├── backend/                 # 后端代码
│   ├── config/             # 数据库配置
│   ├── models/             # 数据模型
│   ├── routes/             # 路由定义
│   ├── middleware/         # 中间件
│   └── server.js           # 服务器入口
├── frontend/               # 前端代码
│   ├── css/               # 样式文件
│   ├── js/                # JavaScript文件
│   ├── index.html         # 首页
│   ├── login.html         # 登录页
│   ├── register.html      # 注册页
│   ├── cart.html          # 购物车页
│   ├── orders.html        # 订单页
│   └── admin/             # 管理员页面
├── package.json           # 项目依赖
└── README.md             # 项目说明
```

### 2.2 关键代码实现

#### 2.2.1 数据库配置（backend/config/db.js）

```javascript
const { Sequelize } = require('sequelize');
require('dotenv').config();

// 创建数据库连接
const sequelize = new Sequelize(
  process.env.DB_NAME,
  process.env.DB_USER,
  process.env.DB_PASSWORD,
  {
    host: process.env.DB_HOST,
    dialect: 'mysql',
    port: 3306,
    pool: {
      max: 10,
      min: 0,
      acquire: 30000,
      idle: 10000
    }
  }
);

// 测试数据库连接
sequelize.authenticate()
  .then(() => {
    console.log('数据库连接成功');
  })
  .catch(err => {
    console.error('数据库连接失败:', err);
  });

module.exports = sequelize;
```

**代码说明：** 使用Sequelize ORM连接MySQL数据库，配置连接池参数，实现数据库连接测试。

#### 2.2.2 用户模型定义（backend/models/User.js）

```javascript
const { DataTypes } = require('sequelize');
const sequelize = require('../config/db');
const bcrypt = require('bcrypt');

const User = sequelize.define('User', {
  id: {
    type: DataTypes.INTEGER,
    primaryKey: true,
    autoIncrement: true
  },
  name: {
    type: DataTypes.STRING,
    allowNull: false
  },
  email: {
    type: DataTypes.STRING,
    allowNull: false,
    unique: true
  },
  password: {
    type: DataTypes.STRING,
    allowNull: false
  },
  phone: {
    type: DataTypes.STRING
  },
  address: {
    type: DataTypes.TEXT
  },
  role: {
    type: DataTypes.ENUM('customer', 'admin'),
    defaultValue: 'customer'
  }
}, {
  hooks: {
    // 密码加密
    beforeCreate: async (user) => {
      if (user.password) {
        const salt = await bcrypt.genSalt(10);
        user.password = await bcrypt.hash(user.password, salt);
      }
    },
    beforeUpdate: async (user) => {
      if (user.changed('password')) {
        const salt = await bcrypt.genSalt(10);
        user.password = await bcrypt.hash(user.password, salt);
      }
    }
  }
});

// 密码验证方法
User.prototype.validatePassword = async function(password) {
  return await bcrypt.compare(password, this.password);
};

module.exports = User;
```

**代码说明：** 定义用户数据模型，使用bcrypt对密码进行哈希加密，实现密码验证方法。

#### 2.2.3 用户认证路由（backend/routes/auth.js）

```javascript
const express = require('express');
const router = express.Router();
const { User } = require('../models');
const { generateToken, authMiddleware } = require('../middleware/auth');

// 用户注册
router.post('/register', async (req, res) => {
  try {
    const { name, email, password, phone, address } = req.body;
    
    // 检查邮箱是否已存在
    const existingUser = await User.findOne({ where: { email } });
    if (existingUser) {
      return res.status(400).json({ message: '该邮箱已被注册' });
    }
    
    // 创建新用户
    const user = await User.create({
      name,
      email,
      password,
      phone,
      address
    });
    
    // 生成JWT令牌
    const token = generateToken(user);
    
    // 返回用户信息和令牌
    res.status(201).json({
      message: '注册成功',
      user: {
        id: user.id,
        name: user.name,
        email: user.email,
        role: user.role
      },
      token
    });
  } catch (error) {
    console.error('注册失败：', error);
    res.status(500).json({ message: '服务器错误', error: error.message });
  }
});

// 用户登录
router.post('/login', async (req, res) => {
  try {
    const { email, password } = req.body;
    
    // 查找用户
    const user = await User.findOne({ where: { email } });
    if (!user) {
      return res.status(401).json({ message: '邮箱或密码错误' });
    }
    
    // 验证密码
    const isPasswordValid = await user.validatePassword(password);
    if (!isPasswordValid) {
      return res.status(401).json({ message: '邮箱或密码错误' });
    }
    
    // 生成JWT令牌
    const token = generateToken(user);
    
    // 返回用户信息和令牌
    res.json({
      message: '登录成功',
      user: {
        id: user.id,
        name: user.name,
        email: user.email,
        role: user.role
      },
      token
    });
  } catch (error) {
    console.error('登录失败：', error);
    res.status(500).json({ message: '服务器错误', error: error.message });
  }
});

module.exports = router;
```

**代码说明：** 实现用户注册和登录功能，包括邮箱验证、密码验证、JWT令牌生成等核心功能。

#### 2.2.4 前端产品展示（frontend/js/products.js）

```javascript
// 页面加载时获取产品列表
window.addEventListener('DOMContentLoaded', function() {
    fetchProducts();
    
    // 搜索按钮点击事件
    const searchBtn = document.getElementById('searchBtn');
    if (searchBtn) {
        searchBtn.addEventListener('click', function() {
            const searchTerm = document.getElementById('searchInput').value;
            searchProducts(searchTerm);
        });
    }
});

// 获取产品列表
async function fetchProducts() {
    try {
        const response = await fetch('http://localhost:3000/api/products');
        if (response.ok) {
            const data = await response.json();
            displayProducts(data.products);
        }
    } catch (error) {
        console.error('获取产品列表失败:', error);
        showMessage('获取产品列表失败，请稍后重试', 'error');
    }
}

// 展示产品列表
function displayProducts(products) {
    const container = document.getElementById('productsContainer');
    
    if (products.length === 0) {
        container.innerHTML = `
            <div class="empty-state">
                <h3>没有找到产品</h3>
                <p>请尝试其他搜索条件</p>
            </div>
        `;
        return;
    }
    
    container.innerHTML = products.map(product => `
        <div class="product-card">
            ${product.image ? `<img src="${product.image}" alt="${product.name}" style="width: 150px; height: 150px; object-fit: cover; margin-bottom: 10px;">` : ''}
            <h3>${product.name}</h3>
            <p class="price">¥${parseFloat(product.price).toFixed(2)}</p>
            <p class="description">${product.description}</p>
            <p class="stock">库存: ${product.stock}</p>
            <button 
                onclick="addToCart(${product.id})" 
                ${product.stock <= 0 ? 'disabled' : ''}
            >
                ${product.stock <= 0 ? '库存不足' : '加入购物车'}
            </button>
        </div>
    `).join('');
}
```

**代码说明：** 前端产品展示功能，包括异步获取产品数据、动态渲染产品卡片、搜索功能等。

### 2.3 项目特色

1. **前后端分离设计：** 前端使用原生技术，后端采用Node.js，提升开发效率
2. **模块化架构：** 清晰的代码结构，易于维护和扩展
3. **安全认证：** JWT token认证，密码加密存储
4. **响应式设计：** 适配不同设备屏幕尺寸
5. **RESTful API：** 规范化的API接口设计

### 2.4 代码地址

**GitHub地址：** https://github.com/chenzikeng/shopping-website.git

---

## 3. 应用部署

### 3.1 部署详细过程

#### 3.1.1 环境准备
1. **服务器环境：** 阿里云ECS服务器
2. **操作系统：** Ubuntu/CentOS Linux
3. **Node.js环境：** 安装Node.js 16+版本
4. **数据库：** MySQL 8.0+数据库

#### 3.1.2 部署步骤

**步骤1：服务器配置**
```bash
# 更新系统包
sudo apt update && sudo apt upgrade -y

# 安装Node.js和npm
curl -fsSL https://deb.nodesource.com/setup_16.x | sudo -E bash -
sudo apt-get install -y nodejs

# 安装MySQL数据库
sudo apt install mysql-server -y

# 安装PM2进程管理器
sudo npm install -g pm2
```

**步骤2：项目部署**
```bash
# 克隆项目代码
git clone https://github.com/chenzikeng/shopping-website.git
cd shopping-website

# 安装后端依赖
cd backend
npm install

# 配置环境变量
cp .env.example .env
# 编辑.env文件，配置数据库连接信息

# 启动数据库
sudo systemctl start mysql
sudo mysql_secure_installation
```

**步骤3：数据库初始化**
```bash
# 创建数据库
mysql -u root -p
CREATE DATABASE shopping_website;
CREATE USER 'shopping_user'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON shopping_website.* TO 'shopping_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;

# 运行数据库迁移脚本
node scripts/init-db.js
```



**步骤4：启动应用**
```bash
# 使用PM2启动后端服务
cd backend
pm2 start server.js --name "shopping-backend"

# 配置PM2开机自启
pm2 startup
pm2 save
```

**步骤5：防火墙配置**
```bash
# 开放必要端口
sudo ufw allow 80
sudo ufw allow 443
sudo ufw allow 22
sudo ufw enable
```

### 3.2 部署地址

**服务器地址：** 8.148.199.180  
**HTTP访问地址：** http://8.148.199.180  
**前端页面地址：** http://8.148.199.180/frontend/  
**API接口地址：** http://8.148.199.180/api/

### 3.3 功能页面访问

- **首页：** http://8.148.199.180/frontend/index.html
- **登录页：** http://8.148.199.180/frontend/login.html
- **注册页：** http://8.148.199.180/frontend/register.html
- **购物车：** http://8.148.199.180/frontend/cart.html
- **我的订单：** http://8.148.199.180/frontend/orders.html
- **管理员页面：** http://8.148.199.180/frontend/admin.html

### 3.4 测试账号和口令

#### 3.4.1 管理员测试账号
- **邮箱：** admin@example.com
- **密码：** admin123
- **权限：** 完整管理功能，包括产品管理、订单管理、销售报表

#### 3.4.2 普通用户注册
- 用户可以通过注册页面创建新账户
- 支持用户自行注册普通用户账户

### 3.5 测试步骤

1. **访问首页：** 打开 http://8.148.199.180/frontend/index.html
2. **用户注册：** 点击注册链接，填写用户信息
3. **用户登录：** 使用注册的邮箱和密码登录
4. **浏览产品：** 查看产品列表，搜索功能测试
5. **添加购物车：** 选择商品加入购物车
6. **下单测试：** 创建订单，查看订单状态
7. **管理员功能：** 使用admin账号登录，测试管理功能

### 3.6 部署总结

本次部署成功实现了购物网站的完整功能，包括用户管理、产品管理、购物车、订单管理等核心功能。系统采用前后端分离架构，具有良好的可扩展性和维护性。部署过程中配置了完整的服务器环境，确保系统稳定运行。

---

## 4. AI辅助完成课程实验的心得体会

### 4.1 如何使用提示词

在本次课程实验中，我深入体验了AI助手在软件开发过程中的辅助作用。通过精心设计的提示词，我能够高效地获取所需的技术支持：

#### 4.1.1 提示词设计策略
- **具体化描述：** 明确指出需要的技术栈、功能模块或问题细节
- **上下文提供：** 完整描述项目背景、现有代码结构和当前需求
- **分步骤提问：** 将复杂任务拆分为多个具体的子任务逐步解决
- **示例引导：** 通过具体的代码示例或错误信息帮助AI理解需求

#### 4.1.2 有效的提示词示例
```
"请为购物网站的实验报告撰写系统设计部分，需要包含：
1. 整体架构图（前后端分离）
2. 技术栈介绍（Node.js + MySQL）
3. 模块功能说明（用户认证、产品管理、购物车等）
4. 数据库表结构设计"
```

### 4.2 对完成任务的效率提升

#### 4.2.1 显著的时间节省
- **代码生成：** 原本需要几小时的CRUD操作代码，在AI辅助下10分钟内完成
- **文档编写：** 实验报告从构思到完成，节省了约60%的时间
- **错误调试：** 快速定位代码问题，提供多种解决方案

#### 4.2.2 质量提升
- **标准化代码：** AI生成的代码符合最佳实践，减少了重构需求
- **完整性检查：** 自动检查遗漏的功能模块和边界情况
- **文档规范：** 统一格式和风格的文档输出

#### 4.2.3 学习效率
- **即时反馈：** 获得实时的代码建议和技术指导
- **知识拓展：** 了解不熟悉的技术领域和实现方案
- **最佳实践：** 学习业界标准的设计模式和开发规范

### 4.3 是否提高自身能力

#### 4.3.1 技术能力提升
- **代码理解能力：** 通过AI的详细解释，更深入理解复杂代码逻辑
- **系统设计能力：** 学习如何架构大型项目，理解各组件之间的关系
- **问题解决能力：** 掌握分析问题、提出解决方案的系统性方法

#### 4.3.2 思维模式转变
- **效率优先：** 学会如何高效利用工具，而不是完全依赖人工操作
- **质量意识：** 更加注重代码质量和项目规范
- **持续学习：** 保持对新技术的敏感度和学习热情

#### 4.3.3 实际能力验证
- **独立完成：** 虽然有AI辅助，但能够独立完成整个项目的开发和部署
- **问题诊断：** 具备独立排查和解决技术问题的能力
- **技术决策：** 能够评估不同技术方案的优劣，做出合理选择

### 4.4 应用的局限性

#### 4.4.1 技术局限性
- **上下文限制：** 对于复杂项目，AI可能无法完全理解全局架构
- **代码依赖：** 过度依赖AI生成的代码，可能影响自主编程能力
- **业务理解：** 对于特定业务场景的理解可能不够深入

#### 4.4.2 质量控制问题
- **代码审查：** AI生成的代码仍需要人工审查和测试
- **安全考虑：** 敏感信息的处理需要特别注意
- **定制化需求：** 特殊需求可能需要大量的提示词调整

#### 4.4.3 依赖性风险
- **工具依赖：** 过度依赖AI工具可能降低独立思考能力
- **技能退化：** 长期依赖可能导致基础编程技能生疏
- **创新限制：** AI倾向于生成常见解决方案，可能限制创新思维

### 4.5 对未来的展望

#### 4.5.1 技术发展趋势
- **智能化程度提升：** AI将更加智能，能够理解更复杂的项目需求
- **多模态协作：** 支持代码、文档、图表等多种形式的交互
- **实时协作：** 实现与开发环境的深度集成，提供实时辅助

#### 4.5.2 教育应用前景
- **个性化学习：** 根据学生的学习进度提供定制化的学习建议
- **技能评估：** 智能评估学生的编程能力和学习效果
- **项目指导：** 提供从规划到实现的全流程智能指导

#### 4.5.3 个人发展建议
- **平衡使用：** 在利用AI提高效率的同时，保持基础技能的练习
- **批判思维：** 对AI的建议保持批判性思考，不盲目接受
- **持续创新：** 培养独立思考和创新能力，避免过度依赖

### 4.6 总结

通过本次课程实验中与AI助手的深入协作，我获得了一次宝贵的学习体验和技能提升机会，这次经历让我深刻认识到，人工智能技术不仅仅是一个工具，更是现代软件开发过程中不可或缺的智能伙伴。在实验过程中，我系统地掌握了提示词设计的核心策略，包括具体化描述需求、提供完整的项目上下文、将复杂任务拆分为具体子步骤以及通过具体示例引导AI理解等方法，这些策略的运用使我能够更高效地与AI进行技术交流。在效率提升方面，这次实验展现出了显著的成果：原本需要数小时完成的CRUD操作代码在AI辅助下缩短至10分钟内完成，实验报告的撰写过程节省了约60%的时间，错误调试效率更是得到了质的提升。这种效率的提升并非简单的"代劳"，而是通过学习AI提供的标准化代码、最佳实践建议和规范化文档，让我将更多精力投入到系统架构设计和创新功能思考中。

通过这次深度合作，我的技术能力得到了多维度的提升。在代码理解方面，AI的详细解释帮助我更深入地掌握了复杂代码逻辑；在系统设计能力上，通过学习AI提供的架构方案和设计模式，我对大型项目的组件关系有了更清晰的认识；在问题解决方面，AI引导的系统性分析方法让我的技术思维更加严谨和全面。同时，我的思维模式也发生了积极转变，从单纯追求功能实现转向注重代码质量和项目规范，从被动接受知识转向主动探索新技术领域。尽管有AI辅助，但我能够独立完成整个项目的开发和部署，具备独立排查和解决技术问题的能力，能够评估不同技术方案的优劣并做出合理选择，这充分说明AI确实提升了我的实际能力而非替代我的思考。

然而，我也清楚地认识到了AI辅助学习应用的局限性。在技术层面，AI对复杂项目的全局架构理解仍有局限，过度依赖可能影响自主编程能力的培养；在质量控制方面，AI生成的代码仍需要人工仔细审查和全面测试，敏感信息处理更是需要特别谨慎；在依赖性风险上，长期过度依赖AI工具可能导致基础编程技能生疏，甚至限制创新思维的发展。这种局限性提醒我，在享受AI带来的效率红利的同时，必须保持清醒的头脑和批判性思维，确保在利用AI提高效率的同时保持基础技能的持续练习。

展望未来，我对人机协作的学习模式充满期待。随着AI技术的不断进步，我预见智能化程度将进一步提升，能够理解更复杂的项目需求；多模态协作将成为现实，支持代码、文档、图表等多种形式的深度交互；实时协作能力将实现与开发环境的无缝集成，提供即时、智能的技术支持。在个人发展层面，我将坚持平衡使用的原则，在利用AI提高效率的同时保持对AI建议的批判性思考，不盲目接受任何技术方案；培养独立思考和创新能力，确保在AI时代保持技术发展的主动权。这次与AI助手的深度合作，不仅让我完成了高质量的课程实验，更重要的是为我指明了在人工智能时代持续学习和技能发展的方向，我相信这种前瞻性的学习体验将成为我未来技术职业生涯的宝贵财富。

---

**报告完成日期：** [请填写完成日期]  
**实验总结：** 通过本次实验，成功实现了完整的购物网站系统，涵盖了现代Web应用的核心功能模块。在AI辅助下提升了开发效率和代码质量，同时深入思考了AI工具在软件开发中的应用价值和局限性，为未来的技术发展提供了宝贵的经验和思考。